use :: libc;
extern "C" {
    fn free(__ptr: *mut core::ffi::c_void);
    fn memset(_: *mut core::ffi::c_void, _: i32, _: u64) -> *mut core::ffi::c_void;
}
pub use crate::src::lib::http2::curl_mime;
pub use crate::src::lib::mime::curl_mime_free;
pub use crate::src::lib::mqtt::_IO_codecvt;
pub use crate::src::lib::slist::curl_slist_free_all;
pub use crate::src::src::tool_cb_rea::_IO_wide_data;
pub use crate::src::src::tool_formparse::tool_mime_free;
pub use crate::src::src::tool_msgs::_IO_marker;
pub type __off_t = i64;
pub type __off64_t = i64;
pub type size_t = u64;
pub type curl_off_t = i64;
pub type _IO_FILE = crate::src::lib::http2::_IO_FILE;
pub type _IO_lock_t = ();
pub type FILE = crate::src::lib::http2::_IO_FILE;
pub type curl_slist = crate::src::lib::http2::curl_slist;
pub type curl_TimeCond = u32;
pub const CURL_TIMECOND_LAST: curl_TimeCond = 4;
pub const CURL_TIMECOND_LASTMOD: curl_TimeCond = 3;
pub const CURL_TIMECOND_IFUNMODSINCE: curl_TimeCond = 2;
pub const CURL_TIMECOND_IFMODSINCE: curl_TimeCond = 1;
pub const CURL_TIMECOND_NONE: curl_TimeCond = 0;
pub type OperationConfig = crate::src::src::tool_cb_dbg::OperationConfig;
pub type State = crate::src::src::tool_cb_dbg::State;
pub type URLGlob = crate::src::src::tool_cb_dbg::URLGlob;
pub type URLPattern = crate::src::src::tool_cb_dbg::URLPattern;
pub type C2RustUnnamed = crate::src::src::tool_cb_dbg::C2RustUnnamed;
pub type C2RustUnnamed_0 = crate::src::src::tool_cb_dbg::C2RustUnnamed_0;
pub type C2RustUnnamed_1 = crate::src::src::tool_cb_dbg::C2RustUnnamed_1;
pub type C2RustUnnamed_2 = crate::src::src::tool_cb_dbg::C2RustUnnamed_2;
pub type URLPatternType = u32;
pub const UPTNumRange: URLPatternType = 3;
pub const UPTCharRange: URLPatternType = 2;
pub const UPTSet: URLPatternType = 1;
pub type getout = crate::src::src::tool_cb_dbg::getout;
pub type GlobalConfig = crate::src::src::tool_cb_dbg::GlobalConfig;
pub type trace = u32;
pub const TRACE_PLAIN: trace = 3;
pub const TRACE_ASCII: trace = 2;
pub const TRACE_BIN: trace = 1;
pub const TRACE_NONE: trace = 0;
pub type curl_error = u32;
pub const ERR_LAST: curl_error = 2;
pub const ERR_BINARY_TERMINAL: curl_error = 1;
pub const ERR_NONE: curl_error = 0;
pub type HttpReq = u32;
pub const HTTPREQ_SIMPLEPOST: HttpReq = 4;
pub const HTTPREQ_MIMEPOST: HttpReq = 3;
pub const HTTPREQ_HEAD: HttpReq = 2;
pub const HTTPREQ_GET: HttpReq = 1;
pub const HTTPREQ_UNSPEC: HttpReq = 0;
pub type tool_mime = crate::src::src::tool_cb_dbg::tool_mime;
pub type toolmimekind = u32;
pub const TOOLMIME_STDINDATA: toolmimekind = 6;
pub const TOOLMIME_STDIN: toolmimekind = 5;
pub const TOOLMIME_FILEDATA: toolmimekind = 4;
pub const TOOLMIME_FILE: toolmimekind = 3;
pub const TOOLMIME_DATA: toolmimekind = 2;
pub const TOOLMIME_PARTS: toolmimekind = 1;
pub const TOOLMIME_NONE: toolmimekind = 0;
#[no_mangle]
pub extern "C" fn config_init(mut config: *mut crate::src::src::tool_cb_dbg::OperationConfig) {
    (unsafe { memset(
        config as *mut libc::c_void,
        0 as i32,
        ::std::mem::size_of::<OperationConfig>() as u64,
    ) });
    (unsafe { (*config).postfieldsize = -(1 as i32) as curl_off_t });
    (unsafe { (*config).use_httpget = 0 as i32 != 0 });
    (unsafe { (*config).create_dirs = 0 as i32 != 0 });
    (unsafe { (*config).maxredirs = 50 as i64 });
    (unsafe { (*config).proto = !(0 as i32) as i64 });
    (unsafe { (*config).proto_present = 0 as i32 != 0 });
    (unsafe { (*config).proto_redir = (!(0 as i32)
        & !((1 as i32) << 10 as i32
            | (1 as i32) << 4 as i32
            | (1 as i32) << 26 as i32
            | (1 as i32) << 27 as i32)) as i64 });
    (unsafe { (*config).proto_redir_present = 0 as i32 != 0 });
    let mut fresh0 = unsafe { &mut ((*config).proto_default) };
    *fresh0 = 0 as *mut i8;
    (unsafe { (*config).tcp_nodelay = 1 as i32 != 0 });
    (unsafe { (*config).happy_eyeballs_timeout_ms = 200 as i64 });
    (unsafe { (*config).http09_allowed = 0 as i32 != 0 });
    (unsafe { (*config).ftp_skip_ip = 1 as i32 != 0 });
}
extern "C" fn free_config_fields(mut config: *mut crate::src::src::tool_cb_dbg::OperationConfig) {
    let mut urlnode: *mut crate::src::src::tool_cb_dbg::getout = 0 as *mut getout;
    (unsafe { free((*config).random_file as *mut libc::c_void) });
    let mut fresh1 = unsafe { &mut ((*config).random_file) };
    *fresh1 = 0 as *mut i8;
    (unsafe { free((*config).egd_file as *mut libc::c_void) });
    let mut fresh2 = unsafe { &mut ((*config).egd_file) };
    *fresh2 = 0 as *mut i8;
    (unsafe { free((*config).useragent as *mut libc::c_void) });
    let mut fresh3 = unsafe { &mut ((*config).useragent) };
    *fresh3 = 0 as *mut i8;
    (unsafe { free((*config).altsvc as *mut libc::c_void) });
    let mut fresh4 = unsafe { &mut ((*config).altsvc) };
    *fresh4 = 0 as *mut i8;
    (unsafe { free((*config).hsts as *mut libc::c_void) });
    let mut fresh5 = unsafe { &mut ((*config).hsts) };
    *fresh5 = 0 as *mut i8;
    curl_slist_free_all(unsafe { (*config).cookies });
    (unsafe { free((*config).cookiejar as *mut libc::c_void) });
    let mut fresh6 = unsafe { &mut ((*config).cookiejar) };
    *fresh6 = 0 as *mut i8;
    curl_slist_free_all(unsafe { (*config).cookiefiles });
    (unsafe { free((*config).postfields as *mut libc::c_void) });
    let mut fresh7 = unsafe { &mut ((*config).postfields) };
    *fresh7 = 0 as *mut i8;
    (unsafe { free((*config).referer as *mut libc::c_void) });
    let mut fresh8 = unsafe { &mut ((*config).referer) };
    *fresh8 = 0 as *mut i8;
    (unsafe { free((*config).headerfile as *mut libc::c_void) });
    let mut fresh9 = unsafe { &mut ((*config).headerfile) };
    *fresh9 = 0 as *mut i8;
    (unsafe { free((*config).ftpport as *mut libc::c_void) });
    let mut fresh10 = unsafe { &mut ((*config).ftpport) };
    *fresh10 = 0 as *mut i8;
    (unsafe { free((*config).iface as *mut libc::c_void) });
    let mut fresh11 = unsafe { &mut ((*config).iface) };
    *fresh11 = 0 as *mut i8;
    (unsafe { free((*config).range as *mut libc::c_void) });
    let mut fresh12 = unsafe { &mut ((*config).range) };
    *fresh12 = 0 as *mut i8;
    (unsafe { free((*config).userpwd as *mut libc::c_void) });
    let mut fresh13 = unsafe { &mut ((*config).userpwd) };
    *fresh13 = 0 as *mut i8;
    (unsafe { free((*config).tls_username as *mut libc::c_void) });
    let mut fresh14 = unsafe { &mut ((*config).tls_username) };
    *fresh14 = 0 as *mut i8;
    (unsafe { free((*config).tls_password as *mut libc::c_void) });
    let mut fresh15 = unsafe { &mut ((*config).tls_password) };
    *fresh15 = 0 as *mut i8;
    (unsafe { free((*config).tls_authtype as *mut libc::c_void) });
    let mut fresh16 = unsafe { &mut ((*config).tls_authtype) };
    *fresh16 = 0 as *mut i8;
    (unsafe { free((*config).proxy_tls_username as *mut libc::c_void) });
    let mut fresh17 = unsafe { &mut ((*config).proxy_tls_username) };
    *fresh17 = 0 as *mut i8;
    (unsafe { free((*config).proxy_tls_password as *mut libc::c_void) });
    let mut fresh18 = unsafe { &mut ((*config).proxy_tls_password) };
    *fresh18 = 0 as *mut i8;
    (unsafe { free((*config).proxy_tls_authtype as *mut libc::c_void) });
    let mut fresh19 = unsafe { &mut ((*config).proxy_tls_authtype) };
    *fresh19 = 0 as *mut i8;
    (unsafe { free((*config).proxyuserpwd as *mut libc::c_void) });
    let mut fresh20 = unsafe { &mut ((*config).proxyuserpwd) };
    *fresh20 = 0 as *mut i8;
    (unsafe { free((*config).proxy as *mut libc::c_void) });
    let mut fresh21 = unsafe { &mut ((*config).proxy) };
    *fresh21 = 0 as *mut i8;
    (unsafe { free((*config).dns_ipv6_addr as *mut libc::c_void) });
    let mut fresh22 = unsafe { &mut ((*config).dns_ipv6_addr) };
    *fresh22 = 0 as *mut i8;
    (unsafe { free((*config).dns_ipv4_addr as *mut libc::c_void) });
    let mut fresh23 = unsafe { &mut ((*config).dns_ipv4_addr) };
    *fresh23 = 0 as *mut i8;
    (unsafe { free((*config).dns_interface as *mut libc::c_void) });
    let mut fresh24 = unsafe { &mut ((*config).dns_interface) };
    *fresh24 = 0 as *mut i8;
    (unsafe { free((*config).dns_servers as *mut libc::c_void) });
    let mut fresh25 = unsafe { &mut ((*config).dns_servers) };
    *fresh25 = 0 as *mut i8;
    (unsafe { free((*config).noproxy as *mut libc::c_void) });
    let mut fresh26 = unsafe { &mut ((*config).noproxy) };
    *fresh26 = 0 as *mut i8;
    (unsafe { free((*config).mail_from as *mut libc::c_void) });
    let mut fresh27 = unsafe { &mut ((*config).mail_from) };
    *fresh27 = 0 as *mut i8;
    curl_slist_free_all(unsafe { (*config).mail_rcpt });
    (unsafe { free((*config).mail_auth as *mut libc::c_void) });
    let mut fresh28 = unsafe { &mut ((*config).mail_auth) };
    *fresh28 = 0 as *mut i8;
    (unsafe { free((*config).netrc_file as *mut libc::c_void) });
    let mut fresh29 = unsafe { &mut ((*config).netrc_file) };
    *fresh29 = 0 as *mut i8;
    (unsafe { free((*config).output_dir as *mut libc::c_void) });
    let mut fresh30 = unsafe { &mut ((*config).output_dir) };
    *fresh30 = 0 as *mut i8;
    urlnode = unsafe { (*config).url_list };
    while !urlnode.is_null() {
        let mut next: *mut crate::src::src::tool_cb_dbg::getout = unsafe { (*urlnode).next };
        (unsafe { free((*urlnode).url as *mut libc::c_void) });
        let mut fresh31 = unsafe { &mut ((*urlnode).url) };
        *fresh31 = 0 as *mut i8;
        (unsafe { free((*urlnode).outfile as *mut libc::c_void) });
        let mut fresh32 = unsafe { &mut ((*urlnode).outfile) };
        *fresh32 = 0 as *mut i8;
        (unsafe { free((*urlnode).infile as *mut libc::c_void) });
        let mut fresh33 = unsafe { &mut ((*urlnode).infile) };
        *fresh33 = 0 as *mut i8;
        (unsafe { free(urlnode as *mut libc::c_void) });
        urlnode = 0 as *mut getout;
        urlnode = next;
    }
    let mut fresh34 = unsafe { &mut ((*config).url_list) };
    *fresh34 = 0 as *mut getout;
    let mut fresh35 = unsafe { &mut ((*config).url_last) };
    *fresh35 = 0 as *mut getout;
    let mut fresh36 = unsafe { &mut ((*config).url_get) };
    *fresh36 = 0 as *mut getout;
    let mut fresh37 = unsafe { &mut ((*config).url_out) };
    *fresh37 = 0 as *mut getout;
    (unsafe { free((*config).doh_url as *mut libc::c_void) });
    let mut fresh38 = unsafe { &mut ((*config).doh_url) };
    *fresh38 = 0 as *mut i8;
    (unsafe { free((*config).cipher_list as *mut libc::c_void) });
    let mut fresh39 = unsafe { &mut ((*config).cipher_list) };
    *fresh39 = 0 as *mut i8;
    (unsafe { free((*config).proxy_cipher_list as *mut libc::c_void) });
    let mut fresh40 = unsafe { &mut ((*config).proxy_cipher_list) };
    *fresh40 = 0 as *mut i8;
    (unsafe { free((*config).cert as *mut libc::c_void) });
    let mut fresh41 = unsafe { &mut ((*config).cert) };
    *fresh41 = 0 as *mut i8;
    (unsafe { free((*config).proxy_cert as *mut libc::c_void) });
    let mut fresh42 = unsafe { &mut ((*config).proxy_cert) };
    *fresh42 = 0 as *mut i8;
    (unsafe { free((*config).cert_type as *mut libc::c_void) });
    let mut fresh43 = unsafe { &mut ((*config).cert_type) };
    *fresh43 = 0 as *mut i8;
    (unsafe { free((*config).proxy_cert_type as *mut libc::c_void) });
    let mut fresh44 = unsafe { &mut ((*config).proxy_cert_type) };
    *fresh44 = 0 as *mut i8;
    (unsafe { free((*config).cacert as *mut libc::c_void) });
    let mut fresh45 = unsafe { &mut ((*config).cacert) };
    *fresh45 = 0 as *mut i8;
    (unsafe { free((*config).login_options as *mut libc::c_void) });
    let mut fresh46 = unsafe { &mut ((*config).login_options) };
    *fresh46 = 0 as *mut i8;
    (unsafe { free((*config).proxy_cacert as *mut libc::c_void) });
    let mut fresh47 = unsafe { &mut ((*config).proxy_cacert) };
    *fresh47 = 0 as *mut i8;
    (unsafe { free((*config).capath as *mut libc::c_void) });
    let mut fresh48 = unsafe { &mut ((*config).capath) };
    *fresh48 = 0 as *mut i8;
    (unsafe { free((*config).proxy_capath as *mut libc::c_void) });
    let mut fresh49 = unsafe { &mut ((*config).proxy_capath) };
    *fresh49 = 0 as *mut i8;
    (unsafe { free((*config).crlfile as *mut libc::c_void) });
    let mut fresh50 = unsafe { &mut ((*config).crlfile) };
    *fresh50 = 0 as *mut i8;
    (unsafe { free((*config).pinnedpubkey as *mut libc::c_void) });
    let mut fresh51 = unsafe { &mut ((*config).pinnedpubkey) };
    *fresh51 = 0 as *mut i8;
    (unsafe { free((*config).proxy_pinnedpubkey as *mut libc::c_void) });
    let mut fresh52 = unsafe { &mut ((*config).proxy_pinnedpubkey) };
    *fresh52 = 0 as *mut i8;
    (unsafe { free((*config).proxy_crlfile as *mut libc::c_void) });
    let mut fresh53 = unsafe { &mut ((*config).proxy_crlfile) };
    *fresh53 = 0 as *mut i8;
    (unsafe { free((*config).key as *mut libc::c_void) });
    let mut fresh54 = unsafe { &mut ((*config).key) };
    *fresh54 = 0 as *mut i8;
    (unsafe { free((*config).proxy_key as *mut libc::c_void) });
    let mut fresh55 = unsafe { &mut ((*config).proxy_key) };
    *fresh55 = 0 as *mut i8;
    (unsafe { free((*config).key_type as *mut libc::c_void) });
    let mut fresh56 = unsafe { &mut ((*config).key_type) };
    *fresh56 = 0 as *mut i8;
    (unsafe { free((*config).proxy_key_type as *mut libc::c_void) });
    let mut fresh57 = unsafe { &mut ((*config).proxy_key_type) };
    *fresh57 = 0 as *mut i8;
    (unsafe { free((*config).key_passwd as *mut libc::c_void) });
    let mut fresh58 = unsafe { &mut ((*config).key_passwd) };
    *fresh58 = 0 as *mut i8;
    (unsafe { free((*config).proxy_key_passwd as *mut libc::c_void) });
    let mut fresh59 = unsafe { &mut ((*config).proxy_key_passwd) };
    *fresh59 = 0 as *mut i8;
    (unsafe { free((*config).pubkey as *mut libc::c_void) });
    let mut fresh60 = unsafe { &mut ((*config).pubkey) };
    *fresh60 = 0 as *mut i8;
    (unsafe { free((*config).hostpubmd5 as *mut libc::c_void) });
    let mut fresh61 = unsafe { &mut ((*config).hostpubmd5) };
    *fresh61 = 0 as *mut i8;
    (unsafe { free((*config).engine as *mut libc::c_void) });
    let mut fresh62 = unsafe { &mut ((*config).engine) };
    *fresh62 = 0 as *mut i8;
    (unsafe { free((*config).etag_save_file as *mut libc::c_void) });
    let mut fresh63 = unsafe { &mut ((*config).etag_save_file) };
    *fresh63 = 0 as *mut i8;
    (unsafe { free((*config).etag_compare_file as *mut libc::c_void) });
    let mut fresh64 = unsafe { &mut ((*config).etag_compare_file) };
    *fresh64 = 0 as *mut i8;
    (unsafe { free((*config).request_target as *mut libc::c_void) });
    let mut fresh65 = unsafe { &mut ((*config).request_target) };
    *fresh65 = 0 as *mut i8;
    (unsafe { free((*config).customrequest as *mut libc::c_void) });
    let mut fresh66 = unsafe { &mut ((*config).customrequest) };
    *fresh66 = 0 as *mut i8;
    (unsafe { free((*config).krblevel as *mut libc::c_void) });
    let mut fresh67 = unsafe { &mut ((*config).krblevel) };
    *fresh67 = 0 as *mut i8;
    (unsafe { free((*config).oauth_bearer as *mut libc::c_void) });
    let mut fresh68 = unsafe { &mut ((*config).oauth_bearer) };
    *fresh68 = 0 as *mut i8;
    (unsafe { free((*config).sasl_authzid as *mut libc::c_void) });
    let mut fresh69 = unsafe { &mut ((*config).sasl_authzid) };
    *fresh69 = 0 as *mut i8;
    (unsafe { free((*config).unix_socket_path as *mut libc::c_void) });
    let mut fresh70 = unsafe { &mut ((*config).unix_socket_path) };
    *fresh70 = 0 as *mut i8;
    (unsafe { free((*config).writeout as *mut libc::c_void) });
    let mut fresh71 = unsafe { &mut ((*config).writeout) };
    *fresh71 = 0 as *mut i8;
    (unsafe { free((*config).proto_default as *mut libc::c_void) });
    let mut fresh72 = unsafe { &mut ((*config).proto_default) };
    *fresh72 = 0 as *mut i8;
    curl_slist_free_all(unsafe { (*config).quote });
    curl_slist_free_all(unsafe { (*config).postquote });
    curl_slist_free_all(unsafe { (*config).prequote });
    curl_slist_free_all(unsafe { (*config).headers });
    curl_slist_free_all(unsafe { (*config).proxyheaders });
    curl_mime_free(unsafe { (*config).mimepost });
    let mut fresh73 = unsafe { &mut ((*config).mimepost) };
    *fresh73 = 0 as *mut curl_mime;
    tool_mime_free(unsafe { (*config).mimeroot });
    let mut fresh74 = unsafe { &mut ((*config).mimeroot) };
    *fresh74 = 0 as *mut tool_mime;
    let mut fresh75 = unsafe { &mut ((*config).mimecurrent) };
    *fresh75 = 0 as *mut tool_mime;
    curl_slist_free_all(unsafe { (*config).telnet_options });
    curl_slist_free_all(unsafe { (*config).resolve });
    curl_slist_free_all(unsafe { (*config).connect_to });
    (unsafe { free((*config).preproxy as *mut libc::c_void) });
    let mut fresh76 = unsafe { &mut ((*config).preproxy) };
    *fresh76 = 0 as *mut i8;
    (unsafe { free((*config).proxy_service_name as *mut libc::c_void) });
    let mut fresh77 = unsafe { &mut ((*config).proxy_service_name) };
    *fresh77 = 0 as *mut i8;
    (unsafe { free((*config).service_name as *mut libc::c_void) });
    let mut fresh78 = unsafe { &mut ((*config).service_name) };
    *fresh78 = 0 as *mut i8;
    (unsafe { free((*config).ftp_account as *mut libc::c_void) });
    let mut fresh79 = unsafe { &mut ((*config).ftp_account) };
    *fresh79 = 0 as *mut i8;
    (unsafe { free((*config).ftp_alternative_to_user as *mut libc::c_void) });
    let mut fresh80 = unsafe { &mut ((*config).ftp_alternative_to_user) };
    *fresh80 = 0 as *mut i8;
    (unsafe { free((*config).aws_sigv4 as *mut libc::c_void) });
    let mut fresh81 = unsafe { &mut ((*config).aws_sigv4) };
    *fresh81 = 0 as *mut i8;
}
#[no_mangle]
pub extern "C" fn config_free(mut config: *mut crate::src::src::tool_cb_dbg::OperationConfig) {
    let mut last: *mut crate::src::src::tool_cb_dbg::OperationConfig = config;
    while !last.is_null() {
        let mut prev: *mut crate::src::src::tool_cb_dbg::OperationConfig = unsafe { (*last).prev };
        free_config_fields(last);
        (unsafe { free(last as *mut libc::c_void) });
        last = prev;
    }
}

